<script lang="ts">
	// import { onInterval } from '../lib';

	// let counter = 0;
	// onInterval(() => counter += 1, 1000);

  // export let callback: any;
  // export let currentExercise: any;
  // export let timeInMinutes: number;

</script>